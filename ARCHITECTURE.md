# 🏗️ Архитектура проекта

## Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                         КЛИЕНТ                              │
│                      (Браузер)                              │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ HTTP/HTTPS
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    NEXT.JS SERVER                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                  FRONTEND (React)                    │   │
│  │  - Server Components                                 │   │
│  │  - Client Components ('use client')                  │   │
│  │  - Styles (CSS Modules)                              │   │
│  │  - Static Assets                                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                  BACKEND (API Routes)                │   │
│  │  - /api/health  → Health check                       │   │
│  │  - /api/hello   → GET/POST endpoints                 │   │
│  │  - /api/*       → Ваши новые endpoints               │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## Как это работает вместе?

### 1. Frontend запрос к Backend

```typescript
// В компоненте (Frontend)
const response = await fetch('/api/hello?name=Alex');
const data = await response.json();

// Обрабатывается в (Backend)
// src/app/api/hello/route.ts
export async function GET(request: Request) {
  // ... логика
  return NextResponse.json({ message: "Hello, Alex!" });
}
```

### 2. Роутинг

Next.js использует файловую систему для роутинга:

```
src/app/
├── page.tsx              → http://localhost:3000/
├── about/page.tsx        → http://localhost:3000/about
└── api/
    ├── health/route.ts   → http://localhost:3000/api/health
    └── hello/route.ts    → http://localhost:3000/api/hello
```

### 3. Рендеринг

**Server Components (по умолчанию)**
- Рендерятся на сервере
- Нет JavaScript на клиенте
- Могут напрямую обращаться к БД
- Лучше для SEO

**Client Components ('use client')**
- Рендерятся на клиенте
- Могут использовать React hooks (useState, useEffect)
- Интерактивные элементы
- Наша главная страница использует это

```typescript
'use client'; // ← Делает компонент клиентским

import { useState } from 'react';

export default function Home() {
  const [state, setState] = useState('');
  // ...
}
```

## Docker архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     DOCKER CONTAINER                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Node.js 20 Alpine (Runtime)                         │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │  Next.js Standalone Server                     │  │   │
│  │  │  - Frontend pages                              │  │   │
│  │  │  - API routes                                  │  │   │
│  │  │  - Static files                                │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  Port: 3000                                                  │
│  User: nextjs (non-root)                                     │
│  Size: ~200-250 MB                                           │
└─────────────────────────────────────────────────────────────┘
         │
         │ HTTP (Port 3000)
         ▼
┌─────────────────────────────────────────────────────────────┐
│                    COOLIFY / REVERSE PROXY                  │
│  - SSL/TLS (Let's Encrypt)                                  │
│  - Domain routing                                           │
│  - Health checks                                            │
└─────────────────────────────────────────────────────────────┘
         │
         │ HTTPS
         ▼
      Internet
```

## Потоки данных

### Frontend → Backend (внутри приложения)

```
Browser
  │
  │ GET /api/hello
  ▼
Next.js Server
  │
  │ Routing
  ▼
API Route Handler
  │ (src/app/api/hello/route.ts)
  │
  │ Process request
  ▼
JSON Response
  │
  │ HTTP 200
  ▼
Browser
  │
  │ Parse JSON
  ▼
React Component Update
```

### External Request → Frontend

```
Browser
  │
  │ GET /
  ▼
Next.js Server
  │
  │ Routing
  ▼
Page Component
  │ (src/app/page.tsx)
  │
  │ Server/Client Render
  ▼
HTML Response
  │
  │ HTTP 200
  ▼
Browser
  │
  │ Hydration (если Client Component)
  ▼
Interactive Page
```

## Деплой на Coolify

```
┌─────────────────────────────────────────────────────────────┐
│                       GIT REPOSITORY                        │
│  (GitHub / GitLab / Gitea)                                  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ git clone / webhook
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                      COOLIFY SERVER                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  1. Clone repository                                 │   │
│  │  2. Detect Dockerfile                                │   │
│  │  3. docker build -t app .                            │   │
│  │  4. docker run -p 3000:3000 app                      │   │
│  │  5. Health check: GET /api/health                    │   │
│  │  6. Setup reverse proxy + SSL                        │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ HTTPS (443)
                       ▼
                  your-domain.com
```

## Масштабирование

### Вертикальное (больше ресурсов)

```yaml
# docker-compose.yml
services:
  app:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
```

### Горизонтальное (больше инстансов)

```
                    Load Balancer
                          │
         ┌────────────────┼────────────────┐
         ▼                ▼                ▼
    Instance 1       Instance 2       Instance 3
    (Container)      (Container)      (Container)
         │                │                │
         └────────────────┴────────────────┘
                          │
                   Shared Database
```

## Взаимодействие компонентов

```typescript
// ═══════════════════════════════════════════════════
// FRONTEND (Client Component)
// ═══════════════════════════════════════════════════

'use client';
import { useState } from 'react';

export default function MyComponent() {
  const [data, setData] = useState(null);
  
  // Вызов Backend API
  const loadData = async () => {
    const res = await fetch('/api/hello?name=User');
    const json = await res.json();
    setData(json);
  };
  
  return (
    <div>
      <button onClick={loadData}>Load</button>
      {data && <div>{data.message}</div>}
    </div>
  );
}

// ═══════════════════════════════════════════════════
// BACKEND (API Route)
// ═══════════════════════════════════════════════════

import { NextResponse } from 'next/server';

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const name = searchParams.get('name');
  
  // Здесь может быть:
  // - Обращение к БД
  // - Вызов внешних API
  // - Бизнес-логика
  
  return NextResponse.json({
    message: `Hello, ${name}!`
  });
}
```

## Среды выполнения

### Development (`npm run dev`)

- Hot reload
- Source maps
- Подробные ошибки
- Медленнее

### Production (`npm start` / Docker)

- Оптимизированный код
- Минификация
- Tree shaking
- Быстрее

### Standalone (Docker)

- Минимальный размер
- Только необходимые файлы
- Включает Node.js runtime
- Готово к деплою

## Безопасность

```
┌─────────────────────────────────────────────────────────────┐
│  Internet                                                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ HTTPS (TLS 1.3)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Coolify Reverse Proxy                                       │
│  - SSL termination                                           │
│  - Rate limiting (опционально)                               │
│  - DDoS protection (опционально)                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ HTTP (внутренняя сеть)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Docker Container                                            │
│  - Non-root user (nextjs)                                    │
│  - Isolated filesystem                                       │
│  - Limited resources                                         │
│  - No privileged mode                                        │
└─────────────────────────────────────────────────────────────┘
```

## Мониторинг

```
Health Check Loop (каждые 30 секунд):

Coolify
  │
  │ GET /api/health
  ▼
Container
  │
  │ Response: {"status":"ok"}
  ▼
Coolify
  │
  │ Is status === "ok"?
  │
  ├─ YES → Container healthy ✅
  │
  └─ NO → Restart container 🔄
```

## Расширение архитектуры

### Добавление БД

```
Next.js App
    │
    ├─ Frontend (React)
    │
    └─ Backend (API Routes)
         │
         │ Database Client
         ▼
    PostgreSQL / MongoDB
```

### Микросервисы

```
Next.js App
    │
    ├─ Frontend (React)
    │
    └─ Backend (API Routes)
         │
         ├─ Internal APIs
         │
         └─ External Services
              ├─ Auth Service
              ├─ Payment Service
              └─ Storage Service
```

## Резюме

✅ **Frontend и Backend в одном приложении**
- Упрощенная разработка
- Общий код и типы
- Одно место деплоя

✅ **Docker контейнеризация**
- Изолированная среда
- Легкий деплой
- Масштабируемость

✅ **Coolify управление**
- Автоматический деплой
- SSL сертификаты
- Мониторинг и логи

---

Архитектура готова к production использованию! 🚀

