# 🚀 Варианты деплоя на Coolify

## Обзор схем деплоя

У вас есть **два варианта** как деплоить приложение на Coolify:

---

## 📦 Вариант A: Coolify собирает из исходников (простой)

### Как работает:

```
Developer
    │
    │ git push
    ▼
GitHub Repository
    │
    │ webhook (опционально)
    ▼
Coolify Server
    │
    ├─ git clone
    ├─ обнаружить Dockerfile
    ├─ docker build
    └─ docker run
    │
    ▼
Приложение запущено ✅
```

### Настройка в Coolify:

1. **New Application** → **Public/Private Repository**
2. Указать Git URL: `https://github.com/username/svoe-client`
3. Branch: `main`
4. Build Pack: **Dockerfile** (автоопределение)
5. Port: `3000`
6. Deploy!

### Плюсы:
- ✅ Простая настройка (2 минуты)
- ✅ Не нужен Docker Registry
- ✅ Автоматический деплой при push (webhook)
- ✅ Исходники всегда на сервере

### Минусы:
- ❌ Сборка на production сервере (нагрузка)
- ❌ Дольше деплой (~2-5 минут каждый раз)
- ❌ Нет версионирования образов
- ❌ Занимает место на сервере

### Когда использовать:
- 👍 Небольшие проекты
- 👍 Один сервер
- 👍 Редкие деплои
- 👍 Хотите максимально простую настройку

---

## 🏭 Вариант B: CI собирает образ → Coolify запускает (профессиональный)

### Как работает:

```
Developer
    │
    │ git push
    ▼
GitHub Repository
    │
    │ trigger
    ▼
GitHub Actions (CI/CD)
    │
    ├─ npm install
    ├─ npm run lint
    ├─ docker build
    ├─ docker test
    └─ docker push → GitHub Container Registry
    │
    │ webhook
    ▼
Coolify Server
    │
    ├─ docker pull готовый образ
    └─ docker run
    │
    ▼
Приложение запущено ✅
```

### Настройка:

#### 1. GitHub Actions уже настроен! ✅

Файл `.github/workflows/docker.yml` автоматически:
- Собирает Docker образ при push в `main`
- Тестирует endpoints
- Публикует в GitHub Container Registry (`ghcr.io`)

#### 2. Сделать пакет публичным (один раз):

```bash
# После первого push в main:
# 1. Перейти на https://github.com/username/svoe-client/pkgs/container/svoe-client
# 2. Package settings → Change visibility → Public
```

#### 3. Настройка в Coolify:

**Option 1: Docker Image**
1. **New Application** → **Docker Image**
2. Image: `ghcr.io/username/svoe-client:main`
3. Port: `3000`
4. Registry: **Public** (или добавить credentials для приватного)
5. Deploy!

**Option 2: Dockerfile с кастомным образом**
- Можно всё равно использовать Git, но образ будет браться из registry

#### 4. Автоматический деплой при новом образе:

В Coolify включить **Auto Deploy on Image Update**:
- Settings → **Watch Tags** → `main`
- Coolify будет проверять registry каждые N минут
- При новом образе - автоматически деплоит

### Плюсы:
- ✅ **Быстрый деплой** (~30 секунд вместо 2-5 минут)
- ✅ Сборка на GitHub (бесплатно, 2000 минут/месяц)
- ✅ **Версионирование** образов (можно откатиться)
- ✅ Тестирование ДО деплоя
- ✅ Можно деплоить на несколько серверов из одного образа
- ✅ Кеширование слоев (быстрее сборка)

### Минусы:
- ❌ Чуть сложнее настройка (5 минут вместо 2)
- ❌ Нужен Docker Registry (но GitHub бесплатно даёт)
- ❌ Занимает место в registry

### Когда использовать:
- 👍 Production проект
- 👍 Частые деплои
- 👍 Несколько серверов/окружений
- 👍 Нужно версионирование
- 👍 Команда разработчиков

---

## 🎯 Сравнение

| Критерий | Вариант A (Source) | Вариант B (Registry) |
|----------|-------------------|---------------------|
| **Скорость деплоя** | 🐢 2-5 мин | 🚀 30 сек |
| **Простота настройки** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Нагрузка на сервер** | ❌ Высокая | ✅ Низкая |
| **Версионирование** | ❌ Нет | ✅ Да |
| **Тестирование** | ⚠️ После деплоя | ✅ До деплоя |
| **Откат версий** | 🐢 Медленно | 🚀 Быстро |
| **Multi-server** | ❌ Сложно | ✅ Легко |

---

## 📝 Текущая настройка проекта

### ✅ Готово для обоих вариантов!

**Для варианта A (Source):**
- `Dockerfile` ✅
- `docker-compose.yml` ✅
- `next.config.ts` (standalone mode) ✅

**Для варианта B (Registry):**
- `.github/workflows/docker.yml` ✅
- Публикация в `ghcr.io` ✅
- Тестирование в CI ✅

---

## 🔧 Детальные инструкции

### Вариант B: Пошаговая настройка

#### Шаг 1: Push в GitHub

```bash
git add .
git commit -m "feat: добавлен CI/CD и Docker"
git push origin main
```

#### Шаг 2: Дождаться сборки

Перейти на: `https://github.com/username/svoe-client/actions`

Должен появиться зелёный ✅ чекмарк

#### Шаг 3: Сделать пакет публичным

```
https://github.com/username?tab=packages
→ Выбрать svoe-client
→ Package settings
→ Change visibility → Public
```

Или добавить в Coolify credentials для приватного registry.

#### Шаг 4: Проверить образ

```bash
# Локально протестировать опубликованный образ
docker pull ghcr.io/username/svoe-client:main
docker run -p 3000:3000 ghcr.io/username/svoe-client:main
curl http://localhost:3000/api/health
```

#### Шаг 5: Настроить Coolify

**Вариант 5.1: Из Docker Image**

1. Coolify → **+ New** → **Application** → **Docker Image**
2. Заполнить:
   ```
   Image: ghcr.io/username/svoe-client:main
   Port: 3000
   ```
3. Settings → **Auto Deploy**: ON
4. Deploy!

**Вариант 5.2: Git + Custom Image (гибрид)**

1. Coolify → **+ New** → **Application** → **Public Repository**
2. Git URL: `https://github.com/username/svoe-client`
3. В Build Settings указать использовать готовый образ
4. Deploy!

#### Шаг 6: Настроить webhook (опционально)

GitHub → Repository → Settings → Webhooks → Add webhook
- Payload URL: (из Coolify)
- Content type: `application/json`
- Events: `push`, `package`

Теперь при каждом push:
1. GitHub Actions соберёт образ
2. Опубликует в registry
3. Webhook уведомит Coolify
4. Coolify скачает и запустит новый образ

---

## 🎨 Гибридный вариант (лучшее из двух миров)

Можно комбинировать:

**Development:**
- Деплоится из Git (вариант A)
- Быстрые эксперименты
- На отдельном dev сервере

**Production:**
- Деплоится из Registry (вариант B)
- Стабильные версии
- С тестированием в CI

---

## 🔐 Приватный Docker Registry

Если хотите приватный registry:

### GitHub Container Registry (приватный)

В Coolify добавить credentials:
```
Registry URL: ghcr.io
Username: ваш-github-username
Password: Personal Access Token с правами read:packages
```

### Docker Hub

```
Registry URL: docker.io
Username: ваш-dockerhub-username
Password: ваш-пароль или access token
```

### Свой Registry

Coolify может работать с любым Docker Registry.

---

## 💡 Рекомендация

Для вашего проекта **рекомендую Вариант B**:

1. ✅ GitHub Actions уже настроен
2. ✅ Бесплатный GitHub Container Registry
3. ✅ Быстрые деплои
4. ✅ Профессиональный CI/CD pipeline
5. ✅ Легко масштабировать

### Начать можно с Варианта A, потом переключиться на B

Coolify позволяет легко переключаться между вариантами!

---

## 📊 Мониторинг

После деплоя:

```bash
# Проверить что образ актуальный
docker inspect ghcr.io/username/svoe-client:main

# Посмотреть все версии
https://github.com/username/svoe-client/pkgs/container/svoe-client

# Откатиться на предыдущую версию
# В Coolify изменить тег с 'main' на конкретный SHA
ghcr.io/username/svoe-client:main-abc1234
```

---

## ❓ FAQ

**Q: Нужно ли платить за GitHub Container Registry?**  
A: Нет, для публичных репозиториев бесплатно. Для приватных - 500MB бесплатно.

**Q: Можно ли использовать оба варианта?**  
A: Да! Можно иметь dev окружение из Git, а prod из Registry.

**Q: Что быстрее?**  
A: Вариант B ~30 секунд деплой vs 2-5 минут в варианте A.

**Q: Какой вариант использует меньше ресурсов сервера?**  
A: Вариант B - сборка происходит на GitHub, а не на вашем сервере.

**Q: Можно ли откатиться на предыдущую версию?**  
A: В варианте B - да, легко. В варианте A - нужно откатывать git коммит и ждать пересборки.

---

Готово! Выбирайте вариант который вам больше подходит 🚀

